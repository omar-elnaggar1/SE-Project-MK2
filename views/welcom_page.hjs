<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory System - Welcome</title>
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/style.css">
    <style>
        body {
            background: linear-gradient(to bottom, #0066cc, #003366);
            color: #fff;
            font-family: 'Arial', sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            margin: 0;
        }
        .welcome-container {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            padding: 50px 80px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            max-width: 600px;
            width: 100%;
        }
        .welcome-container h1 {
            font-size: 3.5rem;
            font-weight: bold;
            margin-bottom: 30px;
            color: #333;
        }
        .welcome-container p {
            font-size: 1.5rem;
            margin-bottom: 40px;
            color: #555;
        }
        .welcome-container .btn {
            font-size: 1.5rem;
            padding: 15px 30px;
            border-radius: 30px;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }
        .welcome-container .btn-primary:hover {
            background-color: #004bb5;
            transform: scale(1.05);
        }
        .footer {
            margin-top: 20px;
            font-size: 1rem;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="welcome-container">
        <h1>Welcome to the Inventory System</h1>
        <p>You are one step away from joining our inventory system. Login to get started!</p>
        <a href="/login" class="btn btn-primary">Login</a>
    </div>
    <div class="footer">
        &copy; 2024 Inventory System. All rights reserved.
    </div>
</body>
</html>
